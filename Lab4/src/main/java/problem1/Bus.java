package problem1;

/**
 * A class that is defined a Bus class.
 */
public class Bus extends Vehicle {

  /**
   * A constructor that creates a new object of Bus.
   *
   * @param ID           String, ID of the Bus
   * @param averageSpeed Float, averageSpeed of the Bus
   * @param maxSpeed     Float, maxSpeed of the Bus
   */
  public Bus(String ID, Float averageSpeed, Float maxSpeed) {
    super(ID, averageSpeed, maxSpeed);
  }
}
